# frozen_string_literal: true

RSpec.describe Apigatewayv2Rack do
  it "has a version number" do
    expect(Apigatewayv2Rack::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
